# Springboot Web Application that tests a real database using SpringData

- Create a mysql database called "poc" (if you want use a different one make sure to update the application.properties file)
- Run the tests 